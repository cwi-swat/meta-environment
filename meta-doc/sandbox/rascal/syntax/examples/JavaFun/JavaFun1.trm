module JavaFun

java fun str factorial(int x) {
   rel[int,int] somerel = {};
   if (x < 0)
	return "0.0";
   double fact = 1.0;
   return (N + 1).toString();
   while(x > 1){
     fact = fact * x;
     x = x - 1;
   }
   return fact.toString();
}