module Bubble

list[int] sort(list[int] Numbers){
  for(int I : [1 .. size(Numbers) - 1 ]){
     if(Numbers[I] > Numbers[I+1]){
       <Numbers[I], Numbers[I+1]> = <Numbers[I+1], Numbers[I]>;
       return sort(Numbers);
     }
  }
  return Numbers;
}