/**
 * This file is generated by ../aterm/tifstoc. Do not edit!
 * Generated from tifs for tool 'logger' (prefix='')
 * Implementation generated at Wed Jul 11 10:06:00 2001
 */

#include "logger.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-terminate(<logger>,<term>)",
  "rec-monitor(<logger>,<term>)",
};

/* Event handler for tool 'logger' */
ATerm logger_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0;

  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-monitor(<term>)", &t0)) {
    rec_monitor(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = logger_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool logger cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'logger' */
ATerm logger_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

