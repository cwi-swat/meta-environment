
process A is
  let N : int, L : list, MAX : int
  in
	N := 1 . MAX := 1000 .
	L := [] .
	if less(N, MAX) then L := join(L, N) . N := add(N,1) fi
	* if equal(N,MAX) then shutdown("") fi
  endlet

toolbus(A)
