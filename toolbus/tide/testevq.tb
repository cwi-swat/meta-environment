
process TEST is
let
  T : testevq,
  N : int
in
  execute(testevq, T?) .
  snd-do(T, start-burst(30)) .
  ( rec-event(T, burst(N?)) .
    printf("received burst nr %d\n", N) .
    snd-ack-event(T, burst(N)) delay(sec(1))
  ) * delta
endlet

tool testevq is { command = "testevq" }

toolbus(TEST)