/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'clock' (prefix='')
 */

#include "clock.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<clock>,readTime)",
  "rec-terminate(<clock>,<term>)",
};

/* Event handler for tool 'clock' */
ATerm clock_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0;

  if(ATmatch(term, "rec-eval(readTime)")) {
    return readTime(conn);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = clock_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool clock cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'clock' */
ATerm clock_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

