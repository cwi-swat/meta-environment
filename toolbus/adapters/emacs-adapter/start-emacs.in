#! /bin/sh

#echo $* 2>&1

if test "x$1" = "x-h"
then
  echo "This script can only be used as a ToolBus tool!"
  exit 0
fi

prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
libdir=@libdir@
datadir=@datadir@

xemacs -load ${datadir}/tb.el -eval "(TBinit \"$*\")"
