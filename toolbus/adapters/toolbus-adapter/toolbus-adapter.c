/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'toolbus-adapter' (prefix='')
 */

#include "toolbus-adapter.h"

#define NR_SIG_ENTRIES	7

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<toolbus-adapter>,toolbus-start(<str>,<list>))",
  "rec-do(<toolbus-adapter>,toolbus-stop(<int>))",
  "rec-eval(<toolbus-adapter>,toolbus-snd-msg(<int>,<term>))",
  "rec-eval(<toolbus-adapter>,toolbus-get-location(<int>))",
  "rec-eval(<toolbus-adapter>,toolbus-get-instances)",
  "rec-ack-event(<toolbus-adapter>,<term>)",
  "rec-terminate(<toolbus-adapter>,<term>)",
};

/* Event handler for tool 'toolbus-adapter' */
ATerm toolbus_adapter_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  int i0;
  char *s0;
  ATerm t0;

  if(ATmatch(term, "rec-eval(toolbus-snd-msg(<int>,<term>))", &i0, &t0)) {
    return toolbus_snd_msg(conn, i0, t0);
  }
  if(ATmatch(term, "rec-do(toolbus-stop(<int>))", &i0)) {
    toolbus_stop(conn, i0);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(toolbus-get-location(<int>))", &i0)) {
    return toolbus_get_location(conn, i0);
  }
  if(ATmatch(term, "rec-eval(toolbus-start(<str>,<term>))", &s0, &t0)) {
    return toolbus_start(conn, s0, t0);
  }
  if(ATmatch(term, "rec-eval(toolbus-get-instances)")) {
    return toolbus_get_instances(conn);
  }
  if(ATmatch(term, "rec-ack-event(<term>)", &t0)) {
    rec_ack_event(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = toolbus_adapter_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool toolbus-adapter cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'toolbus-adapter' */
ATerm toolbus_adapter_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

