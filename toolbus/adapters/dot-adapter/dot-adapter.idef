
process DOT-ADAPTER is
let
  D : dot-adapter,
  Graph : term
in
  execute(dot-adapter, D?) .
  (
    rec-msg(layout-graph(Graph?)) .
    snd-eval(D, layout-graph(Graph)) .
    rec-value(D, graph-layouted(Graph?)) .
    snd-msg(graph-layouted(Graph))
  ) * delta
endlet

tool dot-adapter is { command = "dot-adapter" }

