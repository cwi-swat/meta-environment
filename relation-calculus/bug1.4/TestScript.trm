%% begin declare x : natural, y : natural, natural z;
%%      [s1] x := 3;
%%      if [s2] 3 then
%%         [s3] z := y + x
%%      else
%%         [s4] x := 4
%%      fi
%%      [s5] y := z
%%
%% end

eval(
DEFS    = {<s1, x>, <s3, z>, <s4, x>, <s5, y>}
ISCON   = {<s1, x>}
USES    = {<s3, y>, <s3, x>, <s5, z>}
PRED    = {<s0, s1>,<s1, s2>, <s2, s3>, <s2, s4>, <s3, s5>, <s4, s5>}

UNINITIALIZED = {<$u, $x> in USES | s0 in ({$u} until(DEFS.$x union {s0}, inv PRED))}
)