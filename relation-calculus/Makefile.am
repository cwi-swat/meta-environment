include make_rules

VERSION=0.2

SUBDIRS		= util spec rscripts demo rviz

EXTRA_DIST	= $(wildcard *.pkg) make_rules

noinst_SCRIPTS 	= ${PACKAGE}-${VERSION}.pkg

CLEANFILES = ${PACKAGE}-${VERSION}.pkg
