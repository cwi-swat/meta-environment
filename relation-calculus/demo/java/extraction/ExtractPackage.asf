equations

[extractPackage-1]
	package &Name ; := &PackageDeclaration
	=========================================================
	extractPackage(&PackageDeclaration, &Rel) =
	&Rel union { <name2str(&Name), get-location(&PackageDeclaration)>}
