/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'asfsdfchecker' (prefix='')
 * Implementation generated at Wed Oct 16 11:25:43 2002
 */

#include "asfsdfchecker.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<asfsdfchecker>,check-asfsdf(<term>))",
  "rec-terminate(<asfsdfchecker>,<term>)",
};

/* Event handler for tool 'asfsdfchecker' */
ATerm asfsdfchecker_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0;

  if(ATmatch(term, "rec-eval(check-asfsdf(<term>))", &t0)) {
    return check_asfsdf(conn, t0);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = asfsdfchecker_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool asfsdfchecker cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'asfsdfchecker' */
ATerm asfsdfchecker_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

