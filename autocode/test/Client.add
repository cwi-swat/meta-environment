type Client
{
  name: string
  referrer: Client
  order: list<Order>
}

Client.name->description := "the name of a client"
Client.referrer->description := "the referrer"
Client.order->description := "the list of orders"
