application * {
//{{{ enum Article

enum Article { elements := { pen, pencil, note-book } }

//}}}
//{{{ type Client

type Client
{
  name: string
  peer: Client
  order: list<Order>
}

//}}}
//{{{ type Order

type Order
{
  id: integer
  article: Article
  amount: integer
}

//}}}
}

// Properties
 
//{{{ properties specific to application: server

application server {
  generator := java
  folding := true

  type * {
    access := private
  }

  type Client {
    access := public
    operations := { constructors }
    implements := { Serializable, Runnable }
    throws := { "java.io.IOException" }
  }
}

server.Client->field name {
  access := public
  static := true
}

//}}}
