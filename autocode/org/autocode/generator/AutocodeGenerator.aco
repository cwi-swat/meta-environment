application * {
  generator * {
    enabled := false

    plugin * {
      provides operations {
	type := { }
	field := { }
      }
    }

    type * {
      operation += { }
      description := "no description"
      field * {
        operation += { }
        description := "no description"
      }
    }
  }

  generator java {
    @import "org/autocode/generator/java/JavaGenerator.aco"
  }
}
