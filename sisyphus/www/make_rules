
WWWROOT		= ${top_srcdir}/www

# What gets installed, distributed, cleaned
EXTRA_DIST 	= $(wildcard *.rb) $(wildcard *.rhtml)

# Determine installation directory
wwwdir		= ${pkgdatadir}/www$(subst ./www,,$(subdir))

www_DATA 	= $(EXTRA_DIST)
