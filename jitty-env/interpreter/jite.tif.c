/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'jite' (prefix='')
 * Implementation generated at Tue Jan  7 18:54:16 2003
 */

#include "jite.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<jite>,interpret(<str>,<term>,<term>))",
  "rec-terminate(<jite>,<term>)",
};

/* Event handler for tool 'jite' */
ATerm jite_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  char *s0;
  ATerm t0, t1;

  if(ATmatch(term, "rec-eval(interpret(<str>,<term>,<term>))", &s0, &t0, &t1)) {
    return interpret(conn, s0, t0, t1);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = jite_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool jite cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'jite' */
ATerm jite_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

