/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'addimports' (prefix='')
 * Implementation generated at Tue Jan  7 14:40:14 2003
 */

#include "addimports.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<addimports>,add-imports(<str>,<term>))",
  "rec-terminate(<addimports>,<term>)",
};

/* Event handler for tool 'addimports' */
ATerm addimports_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  char *s0;
  ATerm t0;

  if(ATmatch(term, "rec-eval(add-imports(<str>,<term>))", &s0, &t0)) {
    return add_imports(conn, s0, t0);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = addimports_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool addimports cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'addimports' */
ATerm addimports_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

