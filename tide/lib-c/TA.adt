[
  constructor(Location, line,     line(<file(str)>,<line(int)>)),
  constructor(Location, area,     area-in-file(<file(str)>,area(<start-line(int)>,<start-col(int)>,
                            <end-line(int)>,<end-col(int)>,<offset(int)>,<length(int)>))),
  constructor(Location, line-col, lc(<file(str)>,<line(int)>,<col(int)>)),
  constructor(Location, unknown,  unknown),

  constructor(Port, step,     step),
  constructor(Port, stopped,  stopped),
  constructor(Port, started,  started)
]
