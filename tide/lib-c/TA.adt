[
  [Location, line,     line(<file(str)>,<line(int)>)],
  [Location, area,     line(<file(str)>,<start-line(int)>,<start-col(int)>,
                            <end-line(int)>,<end-col(int)>)],
  [Location, line-col, lc(<file(str)>,<line(int)>,<col(int)>)],

  [Port, step,     step],
  [Port, stopped,  stopped],
  [Port, started,  started]
]
