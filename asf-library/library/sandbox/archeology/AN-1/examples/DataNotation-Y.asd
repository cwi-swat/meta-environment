<module> DataNotation-Y
  <body>

  needs: ActionNotation (yielder) .

  *  _ is _             :: yielder, yielder -> yielder .
  * not _               :: yielder -> yielder .
  * sum _               :: yielder -> yielder .
  * difference _        :: yielder -> yielder .
  * string of _         :: yielder -> yielder .
  * _ ^ _               :: yielder, yielder -> yielder .
</module>
