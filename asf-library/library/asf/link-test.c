#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <MEPT-utils.h>
#include <aterm2.h>

/* DO NOT EDIT: This file is generated */

PT_Tree  set_anno(ATerm tree, PT_Tree input);
PT_Tree  get_anno(ATerm tree, PT_Tree input);

/* This code is not meant for execution, 
 * it is used to test if there is an implementation for every built-in
 */
int main(void)
{

  if (fprintf(stderr, "This program does nothing\n") == 0) {
    set_anno(NULL, NULL);
    get_anno(NULL, NULL);
  }

  return 1; 
}
