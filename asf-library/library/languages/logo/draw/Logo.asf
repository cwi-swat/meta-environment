equations

[Main] LS := runall(Commands, newLogoState),
       Lines := LS.lines
       ==========================================================
       start(Commands, Commands)=start(Document,toSVG(Lines))