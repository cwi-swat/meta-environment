equations

[] fold(f T LF) = foldr(f T LF)

[] foldr(f T []) = T
[] foldr(f T LF) = f(head(LF) foldr(f T tail(LF)))
     when LF != []

[] foldl(f' T []) = T
[] foldl(f' T LF) = foldl(f' f'(T head(LF))  tail(LF))
     when LF != []
