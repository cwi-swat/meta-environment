equations

[] fold(f B LA) = foldr(f B LA)

[] foldr(f B []) = B
[] foldr(f B LA) = f(head(LA) foldr(f B tail(LA)))
     when LA != []

[] foldl(f' B []) = B
[] foldl(f' B LA) = foldl(f' f'(B head(LA))  tail(LA))
     when LA != []
