/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'ptpretty' (prefix='')
 * Implementation generated at Thu Sep 18 09:16:14 2003
 */

#include "ptpretty.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<ptpretty>,format(<tree>,<term>))",
  "rec-terminate(<ptpretty>,<term>)",
};

/* Event handler for tool 'ptpretty' */
ATerm ptpretty_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0, t1;

  if(ATmatch(term, "rec-eval(format(<term>,<term>))", &t0, &t1)) {
    return format(conn, t0, t1);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = ptpretty_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool ptpretty cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'ptpretty' */
ATerm ptpretty_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

