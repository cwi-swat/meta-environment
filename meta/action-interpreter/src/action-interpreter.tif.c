/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'action-interpreter' (prefix='')
 */

#include "action-interpreter.tif.h"

#define NR_SIG_ENTRIES	4

static char *signature[NR_SIG_ENTRIES] = {
  "rec-do(<action-interpreter>,perform-actions(<list>,<list>))",
  "rec-do(<action-interpreter>,process-result(<term>))",
  "rec-ack-event(<action-interpreter>,<term>)",
  "rec-terminate(<action-interpreter>,<term>)",
};

/* Event handler for tool 'action-interpreter' */
ATerm action_interpreter_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0, t1;

  if(ATmatch(term, "rec-do(perform-actions(<term>,<term>))", &t0, &t1)) {
    perform_actions(conn, t0, t1);
    return NULL;
  }
  if(ATmatch(term, "rec-do(process-result(<term>))", &t0)) {
    process_result(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-ack-event(<term>)", &t0)) {
    rec_ack_event(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = action_interpreter_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool action-interpreter cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'action-interpreter' */
ATerm action_interpreter_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

