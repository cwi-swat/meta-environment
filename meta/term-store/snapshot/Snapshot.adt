[
  [Snapshot, Main, term-store(<tables(Tables)>)],

  [Tables, Empty, []],
  [Tables, Many,[<head(Table)>,<[tail(Tables)]>]],

  [Table, default, table(<name(str)>,<value-type(str)>,<rows(Rows)>)],

  [Rows, Empty, []],
  [Rows, Many, [<head(Row)>,<[tail(Rows)]>]],

  [Row, default, row(<key(term)>,<value(term)>)]
]
