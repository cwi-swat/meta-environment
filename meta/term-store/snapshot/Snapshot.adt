[
  constructor(Snapshot,Main,term-store(<tables(Tables)>)),

  list(Tables,Table),

  constructor(Table,default,table(<name(str)>,<value-type(str)>,<rows(Rows)>)),

  list(Rows,Row),

  constructor(Row,default,row(<key(term)>,<value(term)>))
]
