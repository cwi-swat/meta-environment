/**
 * This file is generated by /home/markvdb/Research/bin/tifstoc. Do not edit!
 * Generated from tifs for tool 'findSortDefinition' (prefix='')
 * Implementation generated at Wed Jan 29 14:52:37 2003
 */

#include "findSortDefinition.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<findSortDefinition>,query(<term>,<term>))",
  "rec-terminate(<findSortDefinition>,<term>)",
};

/* Event handler for tool 'findSortDefinition' */
ATerm findSortDefinition_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0, t1;

  if(ATmatch(term, "rec-eval(query(<term>,<term>))", &t0, &t1)) {
    return query(conn, t0, t1);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = findSortDefinition_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool findSortDefinition cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'findSortDefinition' */
ATerm findSortDefinition_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

