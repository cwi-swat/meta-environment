/**
 * This file is generated by /ufs/markvdb/Research/bin/tifstoc. Do not edit!
 * Generated from tifs for tool 'term-base' (prefix='')
 * Implementation generated at Thu May  8 11:13:57 2003
 */

#include "term-base.tif.h"

#define NR_SIG_ENTRIES	0

static char *signature[NR_SIG_ENTRIES] = {
};

/* Event handler for tool 'term-base' */
ATerm term_base_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */

  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = term_base_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool term-base cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'term-base' */
ATerm term_base_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

