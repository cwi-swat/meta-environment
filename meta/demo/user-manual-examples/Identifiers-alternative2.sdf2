module Identifiers-alternative2

imports Layout

exports
  sorts UCLetter LCLetter Digit
  lexical syntax
    [A-Z]   -> UCLetter
    [a-z]   -> LCLetter
    [0-9]   -> Digit

    (UcLetter LCLetter*) | (UCLetter UCLetter*) | (UCLetter Digit*) -> Id
