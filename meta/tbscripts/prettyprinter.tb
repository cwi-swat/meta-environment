/*
  $Id$
 */

tool asource is {
  command = "asource"
}

process Prettyprinter is
let
  PP: asource,
  T: term, 
  Src: term 
in
  execute(asource,PP?).
  ( rec-msg(pp(T?)).
    snd-eval(PP,conssource(T)).
    rec-value(PP,source(Src?)).
    snd-msg(source(Src)) 
  ) *
  delta
endlet
