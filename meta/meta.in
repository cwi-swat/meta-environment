#!/bin/sh
prefix=@prefix@
libdir=@prefix@/lib
classdir=@prefix@/classes
toolbus=@TOOLBUS@/bin/toolbus

CLASSPATH=${classdir}:${CLASSPATH}; export CLASSPATH

echo ${CLASSPATH}
$toolbus $@ -I${libdir} ${libdir}/meta.tb
