/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'se'
 * Implementation generated at Mon Jul  3 11:54:50 2000
 */

#include "se.tif.h"

#define NR_SIG_ENTRIES	16

static char *signature[NR_SIG_ENTRIES] = {
  "rec-terminate(<se>,<term>)",
  "rec-do(<se>,remove-tree(<term>))",
  "rec-eval(<se>,calcErrorLocation(<term>,<list>))",
  "rec-eval(<se>,getDirtyFocuses(<term>))",
  "rec-eval(<se>,makeFocusSort(<str>,<term>))",
  "rec-eval(<se>,registerSymbols(<term>,<list>))",
  "rec-eval(<se>,getTree(<term>))",
  "rec-eval(<se>,setFocusAtLocation(<term>,<int>))",
  "rec-eval(<se>,makeTreeInvalid(<term>))",
  "rec-eval(<se>,moveFocus(<term>,<str>))",
  "rec-do(<se>,setMultipleFocus(<term>,<list>))",
  "rec-do(<se>,replaceFocus(<term>,<term>,<term>))",
  "rec-eval(<se>,deleteChars(<term>,<int>,<int>))",
  "rec-eval(<se>,insertChars(<term>,<int>,<str>))",
  "rec-eval(<se>,newEditorGivenTree(<term>,<term>))",
  "rec-eval(<se>,newEditor(<term>,<str>))",
};

/* Event handler for tool 'se' */
ATerm se_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  int i0, i1;
  char *s0;
  ATerm t0, t1, t2;

  if(ATmatch(term, "rec-eval(makeTreeInvalid(<term>))", &t0)) {
    return makeTreeInvalid(conn, t0);
  }
  if(ATmatch(term, "rec-eval(setFocusAtLocation(<term>,<int>))", &t0, &i0)) {
    return setFocusAtLocation(conn, t0, i0);
  }
  if(ATmatch(term, "rec-eval(moveFocus(<term>,<str>))", &t0, &s0)) {
    return moveFocus(conn, t0, s0);
  }
  if(ATmatch(term, "rec-eval(getTree(<term>))", &t0)) {
    return getTree(conn, t0);
  }
  if(ATmatch(term, "rec-do(setMultipleFocus(<term>,<term>))", &t0, &t1)) {
    setMultipleFocus(conn, t0, t1);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(registerSymbols(<term>,<term>))", &t0, &t1)) {
    return registerSymbols(conn, t0, t1);
  }
  if(ATmatch(term, "rec-do(replaceFocus(<term>,<term>,<term>))", &t0, &t1, &t2)) {
    replaceFocus(conn, t0, t1, t2);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(makeFocusSort(<str>,<term>))", &s0, &t0)) {
    return makeFocusSort(conn, s0, t0);
  }
  if(ATmatch(term, "rec-eval(deleteChars(<term>,<int>,<int>))", &t0, &i0, &i1)) {
    return deleteChars(conn, t0, i0, i1);
  }
  if(ATmatch(term, "rec-eval(getDirtyFocuses(<term>))", &t0)) {
    return getDirtyFocuses(conn, t0);
  }
  if(ATmatch(term, "rec-eval(insertChars(<term>,<int>,<str>))", &t0, &i0, &s0)) {
    return insertChars(conn, t0, i0, s0);
  }
  if(ATmatch(term, "rec-eval(calcErrorLocation(<term>,<term>))", &t0, &t1)) {
    return calcErrorLocation(conn, t0, t1);
  }
  if(ATmatch(term, "rec-eval(newEditorGivenTree(<term>,<term>))", &t0, &t1)) {
    return newEditorGivenTree(conn, t0, t1);
  }
  if(ATmatch(term, "rec-do(remove-tree(<term>))", &t0)) {
    remove_tree(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(newEditor(<term>,<str>))", &t0, &s0)) {
    return newEditor(conn, t0, s0);
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = se_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool se cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'se' */
ATerm se_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

