/**
 * This file is generated by /ufs/jurgenv/glt/bin/tifstoc. Do not edit!
 * Generated from tifs for tool 'editor-manager' (prefix='')
 * Implementation generated at Mon May 26 21:06:19 2003
 */

#include "editor-manager.tif.h"

#define NR_SIG_ENTRIES	6

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<editor-manager>,get-editor-id(<str>,<str>))",
  "rec-eval(<editor-manager>,check-editor-id(<str>,<str>))",
  "rec-eval(<editor-manager>,get-editor-name(<term>))",
  "rec-eval(<editor-manager>,get-editors-by-module(<str>))",
  "rec-do(<editor-manager>,delete-editor(<term>))",
  "rec-terminate(<editor-manager>,<term>)",
};

/* Event handler for tool 'editor-manager' */
ATerm editor_manager_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  char *s0, *s1;
  ATerm t0;

  if(ATmatch(term, "rec-eval(get-editor-name(<term>))", &t0)) {
    return get_editor_name(conn, t0);
  }
  if(ATmatch(term, "rec-eval(check-editor-id(<str>,<str>))", &s0, &s1)) {
    return check_editor_id(conn, s0, s1);
  }
  if(ATmatch(term, "rec-eval(get-editors-by-module(<str>))", &s0)) {
    return get_editors_by_module(conn, s0);
  }
  if(ATmatch(term, "rec-eval(get-editor-id(<str>,<str>))", &s0, &s1)) {
    return get_editor_id(conn, s0, s1);
  }
  if(ATmatch(term, "rec-do(delete-editor(<term>))", &t0)) {
    delete_editor(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = editor_manager_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool editor-manager cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'editor-manager' */
ATerm editor_manager_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

