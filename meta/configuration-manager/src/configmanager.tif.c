/**
 * This file is generated by /ufs/jurgenv/glt/bin/tifstoc. Do not edit!
 * Generated from tifs for tool 'configmanager' (prefix='')
 * Implementation generated at Sat Apr 26 15:58:18 2003
 */

#include "configmanager.tif.h"

#define NR_SIG_ENTRIES	6

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<configmanager>,process-button-file(<str>,<term>))",
  "rec-eval(<configmanager>,process-button-file(<str>,<str>))",
  "rec-eval(<configmanager>,process-config-file(<str>,<str>))",
  "rec-eval(<configmanager>,get-button-names(<str>,<str>))",
  "rec-eval(<configmanager>,get-button-actions(<list>,<str>,<str>))",
  "rec-terminate(<configmanager>,<term>)",
};

/* Event handler for tool 'configmanager' */
ATerm configmanager_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  char *s0, *s1;
  ATerm t0;

  if(ATmatch(term, "rec-eval(process-config-file(<str>,<str>))", &s0, &s1)) {
    return process_config_file(conn, s0, s1);
  }
  if(ATmatch(term, "rec-eval(get-button-names(<str>,<str>))", &s0, &s1)) {
    return get_button_names(conn, s0, s1);
  }
  if(ATmatch(term, "rec-eval(process-button-file(<str>,<term>))", &s0, &t0)) {
    return process_button_file(conn, s0, t0);
  }
  if(ATmatch(term, "rec-eval(get-button-actions(<term>,<str>,<str>))", &t0, &s0, &s1)) {
    return get_button_actions(conn, t0, s0, s1);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = configmanager_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool configmanager cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'configmanager' */
ATerm configmanager_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

