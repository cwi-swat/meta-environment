/**
 * This file is generated by /export/scratch1/jong/meta/install/bin/tifstoc. Do not edit!
 * Generated from tifs for tool 'configmanager' (prefix='')
 * Implementation generated at Fri Aug  1 10:04:10 2003
 */

#include "configmanager.tif.h"

#define NR_SIG_ENTRIES	10

static char *signature[NR_SIG_ENTRIES] = {
  "rec-do(<configmanager>,set-actions(<str>))",
  "rec-do(<configmanager>,remove-actions)",
  "rec-do(<configmanager>,add-predefined-actions(<str>))",
  "rec-eval(<configmanager>,parse-config-file(<str>))",
  "rec-do(<configmanager>,register-user-directories(<list>))",
  "rec-do(<configmanager>,register-library(<str>))",
  "rec-eval(<configmanager>,get-button-names(<str>,<term>))",
  "rec-eval(<configmanager>,get-button-actions(<str>,<term>,<term>))",
  "rec-eval(<configmanager>,get-search-paths)",
  "rec-terminate(<configmanager>,<term>)",
};

/* Event handler for tool 'configmanager' */
ATerm configmanager_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  char *s0;
  ATerm t0, t1;

  if(ATmatch(term, "rec-do(register-user-directories(<term>))", &t0)) {
    register_user_directories(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(parse-config-file(<str>))", &s0)) {
    return parse_config_file(conn, s0);
  }
  if(ATmatch(term, "rec-do(register-library(<str>))", &s0)) {
    register_library(conn, s0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(add-predefined-actions(<str>))", &s0)) {
    add_predefined_actions(conn, s0);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(get-button-names(<str>,<term>))", &s0, &t0)) {
    return get_button_names(conn, s0, t0);
  }
  if(ATmatch(term, "rec-do(remove-actions)")) {
    remove_actions(conn);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(get-button-actions(<str>,<term>,<term>))", &s0, &t0, &t1)) {
    return get_button_actions(conn, s0, t0, t1);
  }
  if(ATmatch(term, "rec-do(set-actions(<str>))", &s0)) {
    set_actions(conn, s0);
    return NULL;
  }
  if(ATmatch(term, "rec-eval(get-search-paths)")) {
    return get_search_paths(conn);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = configmanager_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool configmanager cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'configmanager' */
ATerm configmanager_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

