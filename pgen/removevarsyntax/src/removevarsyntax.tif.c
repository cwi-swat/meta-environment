/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'removevarsyntax' (prefix='')
 * Implementation generated at Wed Oct 16 17:19:50 2002
 */

#include "removevarsyntax.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<removevarsyntax>,remove-var-syntax(<str>,<term>))",
  "rec-terminate(<removevarsyntax>,<term>)",
};

/* Event handler for tool 'removevarsyntax' */
ATerm removevarsyntax_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  char *s0;
  ATerm t0;

  if(ATmatch(term, "rec-eval(remove-var-syntax(<str>,<term>))", &s0, &t0)) {
    return remove_var_syntax(conn, s0, t0);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = removevarsyntax_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool removevarsyntax cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'removevarsyntax' */
ATerm removevarsyntax_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

