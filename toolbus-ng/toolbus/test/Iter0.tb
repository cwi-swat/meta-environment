process P is
let N : int
in
    N := 0 .
    printf("before\n") .
    ( if less(N, 5)
      then
         printf("%d\n", N) . N := add(N, 1)
      fi
    ) * 
    if equal (N, 5) then tau fi
endlet

toolbus(P)

