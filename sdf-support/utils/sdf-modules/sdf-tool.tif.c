/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'sdf-tool' (prefix='')
 * Implementation generated at Thu May 15 18:20:24 2003
 */

#include "sdf-tool.tif.h"

#define NR_SIG_ENTRIES	1

static char *signature[NR_SIG_ENTRIES] = {
  "rec-terminate(<sdf-tool>,<term>)",
};

/* Event handler for tool 'sdf-tool' */
ATerm sdf_tool_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0;

  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = sdf_tool_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool sdf-tool cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'sdf-tool' */
ATerm sdf_tool_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

