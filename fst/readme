This repository was created by:
 cvs -d /ufs/meta-env/CVS import software/fst a b

To checkout:
 setenv CVS_RSH ssh2
 cvs -d ralf@ssh.cwi.nl:/ufs/meta-env/CVS checkout fst
This will create a directory fst.

To commit:
 cvs commit
That should be done from withing the directory fst.
 >??

This is a test, doing cvs commit to see if I can change a file
