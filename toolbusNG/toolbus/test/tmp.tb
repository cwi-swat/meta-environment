process P is 
let Stack : list, NewStack : list, Top : str
in
StackPushString([], NewStack?, "abc")
endlet

process StackPushTerm(Stack : list, NewStack : list?, Top : term) is
let Top1 : term, X : term in
  Top1 := Top .
  X := quote(xxx(3)) .
  NewStack := join(Top, Stack) .
  printf("StackPushTerm: Top = %t, Top1 = %t, X = %t, NewStack = %t\n", Top, Top1, X, NewStack)   %%
endlet
   
process StackPushString(Stack : list, NewStack : list?, Top : str) is
  StackPushTerm(Stack,NewStack?,str(Top))  %% quote

toolbus(P)
