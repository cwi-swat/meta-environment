process P(N : int)
is let X : int in
  subscribe(update(<int>)) .
  rec-note(update(X?)) .
  printf("P %d got note %d\n", N, X)
  endlet
  
process Q is snd-note(update(100))

toolbus(P(1), P(2), P(3), Q)