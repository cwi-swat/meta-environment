/**
 * This file is generated by tifstoc. Do not edit!
 * Generated from tifs for tool 'unparser' (prefix='')
 */

#include "unparser.tif.h"

#define NR_SIG_ENTRIES	2

static char *signature[NR_SIG_ENTRIES] = {
  "rec-eval(<unparser>,unparse-asfix(<term>))",
  "rec-terminate(<unparser>,<term>)",
};

/* Event handler for tool 'unparser' */
ATerm unparser_handler(int conn, ATerm term)
{
  ATerm in, out;
  /* We need some temporary variables during matching */
  ATerm t0;

  if(ATmatch(term, "rec-eval(unparse-asfix(<term>))", &t0)) {
    return unparse_asfix(conn, t0);
  }
  if(ATmatch(term, "rec-terminate(<term>)", &t0)) {
    rec_terminate(conn, t0);
    return NULL;
  }
  if(ATmatch(term, "rec-do(signature(<term>,<term>))", &in, &out)) {
    ATerm result = unparser_checker(conn, in);
    if(!ATmatch(result, "[]"))
      ATfprintf(stderr, "warning: not in input signature:\n\t%\n\tl\n", result);
    return NULL;
  }

  ATerror("tool unparser cannot handle term %t", term);
  return NULL; /* Silence the compiler */
}

/* Check the signature of the tool 'unparser' */
ATerm unparser_checker(int conn, ATerm siglist)
{
  return ATBcheckSignature(siglist, signature, NR_SIG_ENTRIES);
}

